#include "main.h"

/**
 * _abs - computes absolute of an integer
 * @n: absolute value to be computed
 * Return: absolute value of an integer
 */

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
